require "spec_helper"

RSpec.describe Drb::Websockets do
  it "has a version number" do
    expect(Drb::Websockets::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
